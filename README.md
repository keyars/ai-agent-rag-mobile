# ai-agent-rag-mobile
AI Agent + RAG Mobile Assistant - A small, controlled agent that can Understand a user's question → retrieve relevant knowledge → decide whether it needs a tool → execute the tool → reason over the result → produce a grounded answer.
