# Evaluation Scripts

`rag-eval.ts` runs the deterministic retrieval regression suite using the built-in Demo provider. It does not require network access or API credentials.

The CI command is `npm run test:rag` and should remain deterministic so retrieval regressions are visible before release.
